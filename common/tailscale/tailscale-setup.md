# 🌐 Tailscale Setup
Tailscale allows us to access our nodes securely from anywhere.

## Installation
Run this command on the new node:
`curl -fsSL https://tailscale.com/install.sh | sh`

## Authentication
To link the device to your account:
`sudo tailscale up`

Follow the link provided in the terminal to log in.
